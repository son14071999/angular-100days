import { FormatInfoPipe } from './format-info.pipe';

describe('FormatInfoPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatInfoPipe();
    expect(pipe).toBeTruthy();
  });
});
